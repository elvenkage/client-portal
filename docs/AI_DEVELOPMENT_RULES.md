# AI Development Rules

AI agents must follow these rules.

1. Business logic must exist in service classes.

2. Livewire components must contain only UI logic.

3. Database schema must follow DATABASE_SCHEMA.md.

4. AI must not create new tables without updating documentation.

5. AI must check docs before implementing features.

6. AI must not modify architecture files without explanation.